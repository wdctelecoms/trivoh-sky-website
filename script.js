const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.desktop-nav');menuBtn?.addEventListener('click',()=>{const open=menuBtn.getAttribute('aria-expanded')==='true';menuBtn.setAttribute('aria-expanded',String(!open));nav.classList.toggle('mobile-open');});
const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const playBtn=document.getElementById('playBtn');let playing=false;playBtn?.addEventListener('click',()=>{playing=!playing;playBtn.textContent=playing?'Ⅱ':'▶';playBtn.style.transform=playing?'scale(1.08)':'scale(1)';});
const waveform=document.getElementById('waveform');if(waveform){for(let i=0;i<70;i++){const b=document.createElement('span');b.style.display='inline-block';b.style.width='2px';b.style.height=`${8+Math.random()*22}px`;b.style.marginRight='3px';b.style.verticalAlign='middle';b.style.background='currentColor';waveform.appendChild(b)}}
const links=document.querySelectorAll('a[href^="#"]');links.forEach(link=>link.addEventListener('click',e=>{const id=link.getAttribute('href');if(id&&id!=='#'){const target=document.querySelector(id);if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth'});if(nav?.classList.contains('mobile-open')){nav.classList.remove('mobile-open');menuBtn?.setAttribute('aria-expanded','false')}}}}));
